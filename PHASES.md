# PHASES.md

Status of the 14 phases in `TOTETRACK-BUILD-PLAN.md`. "Exit" is the plan's exit criterion.

| #   | Phase                           | Status                       | Exit criterion — how it was verified                                                                                                                                                                                              |
| --- | ------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Scaffold & tooling              | ✅ done                      | `pnpm dev` serves web (:5173) + API (:3000); `/api/health` → `{ok:true}`                                                                                                                                                          |
| 2   | Database & migrations           | ✅ done                      | `drizzle/0000_initial.sql`; migrator runs on boot; `pnpm db:seed` populates 2 series / 3 locations / 3 boxes                                                                                                                      |
| 3   | Auth (PIN)                      | ✅ done                      | `test/auth.test.ts`: 401 before login, setup/login/logout, tampered cookie, 5/15min rate limit, PIN change keeps other sessions; generation rotation signs out everywhere                                                         |
| 4   | Series / locations / boxes CRUD | ✅ done                      | `test/crud.test.ts` incl. 25 concurrent creates → unique contiguous numbers                                                                                                                                                       |
| 5   | Core mobile UI shell            | ✅ done                      | Bottom nav (Search/Boxes/Add/Places/Settings), box list, create/edit, location + series managers; Playwright run at iPhone 13 viewport                                                                                            |
| 6   | Photo upload & gallery          | ✅ done                      | `test/photos-ai.test.ts`: upload → 400px WebP thumb → embla gallery/lightbox → reorder → delete; files 401 without session                                                                                                        |
| 7   | QR deep links & box page        | ✅ done                      | `/b/:labelId` (case-insensitive) → login with `returnTo` → box; unknown label → "Create box A-014"                                                                                                                                |
| 8   | Label PDF generation            | ✅ done                      | pdf-lib + qrcode, Avery 5163 default (+5160), start offset, calibration page, marks printed; `test/labels-export.test.ts`                                                                                                         |
| 9   | Anthropic vision analysis       | ✅ done                      | In-process queue (concurrency 1, recovers pending on boot), JSON prompt + defensive parse, per-photo & box-level re-run, error path — all covered by tests with a stubbed model. **Not exercised against the live API (no key).** |
| 10  | Items editing                   | ✅ done                      | Inline edit name/qty/note, add, delete, "Clear AI items", AI/manual badges, autosaving description                                                                                                                                |
| 11  | Search (FTS)                    | ✅ done                      | `test/items-search.test.ts`: item name/note, box name, location, description with headline, partial label `A-1`/`a12`, filters                                                                                                    |
| 12  | CSV export & settings           | ✅ done                      | `boxes.csv` / `items.csv` / combined `inventory.csv` with escaping tests; Settings page (PIN, AI model + auto toggle, default template, exports)                                                                                  |
| 13  | Docker & deployment             | ✅ done                      | Distroless multi-stage image built and run via `docker-compose.yml` on a clean project: migrations, static SPA, uploads on the volume, restart persistence. README has tunnel + backup notes.                                     |
| 14  | Hardening & polish              | ✅ done (Lighthouse not run) | helmet, file-type sniffing, pino, 404 + error boundary, skeletons, empty states, lazy images. See smoke-test checklist in STATE.md.                                                                                               |
